version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: poly_user
      POSTGRES_PASSWORD: poly_password
      POSTGRES_DB: poly_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save 60 1 --loglevel warning
volumes:
  postgres_data:
  redis_data:
