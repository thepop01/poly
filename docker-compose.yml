version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: poly_user
      POSTGRES_PASSWORD: poly_password
      POSTGRES_DB: poly_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
