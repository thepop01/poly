# Changelog

- 2026-09-04 — **Position→market linkage repair (autonomous run):** Position sync workers now persist market metadata at sync time (`market_metadata.py`, zero extra API calls, blanks-only overwrite). Backfilled ~57,000 missing markets value-ranked (opens by position value, closed by |realized|): open-missing rows 476k→339k (−29%), closed-missing rows 23.7M→17.4M (−27%), ~54k distinct dead-tail markets resolved. Recomputed 15k holders of newly linked high-value markets (+ Worker B cycle covers the rest). Concurrency measured: 20 optimal, 40+ throttles. Progress files make all three backfills resumable; remaining tail is overwhelmingly dead markets.
- 2026-09-04 — **Full league taxonomy from Gamma series:** League filters previously showed only IPL (or blank) because leagues came solely from a small tag map. `markets_v2.league` now falls back to the Gamma event `series` title (canonicalized through the tag map, e.g. "Indian Premier League" → IPL), plus new cricket title rules. Backfilled 697 Cricket markets (International Cricket, IPL, T20 Blast, Major League Cricket, Pakistan Super League, …) with a cross-subcategory guard, and recomputed ~14.5k affected wallets via Worker B. IPL alone now has 46 wallets clearing >70% win rate with ≥20 resolved.
- 2026-09-04 — **Research Hub LLM wiring + scope honesty:** Added `OPENAI_BASE_URL` / `RESEARCH_PROVIDER` (`responses` or `chat-completions`) so any OpenAI-compatible gateway works; missing keys now fail as actionable `PROVIDER_NOT_CONFIGURED`. Scope filters match case-insensitively (`Sports` finds stored `SPORTS`); empty wallet discovery returns `available_scopes` taxonomy hints rendered in the wallet panel; identical repeat tool calls short-circuit instead of burning budget; chat rail shows an animated thinking indicator while busy.
- 2026-09-04 — **AI Research Hub:** New persistent white-theme research workspace at `/hub` where each top-level tab is an independent chat owning its messages, named result sets, panels, and layout. The FastAPI backend owns chat persistence (`research_chats/messages/runs/result_sets/result_members/panels`, migration `w6x7y8z9a0b1`), six validated analytical tools (wallet discovery, markets traded, open-position overlap, market participants, outcome consensus, historical same-outcome), immutable result-set snapshots, and a provider-independent orchestration loop streaming NDJSON (`run.started` → `run.completed`/`run.failed`, exactly one terminal event). Panels auto-arrange on a 12-column grid with deterministic update-or-create keys; the model selects only registered tools and never sees credentials or SQL. Guardrails: strict `>` win-rate comparison, default 20-resolved evidence floor, category scope at `window_size = 0`, open positions require positive unresolved value, history honors `metrics_eligible`, exact-coverage "all" rule, real outcome labels, no coordination claims, per-user run limits, and cross-owner 404s. Verified by 57 backend tests, 25 frontend tests, query-plan guardrails, and two end-to-end conversational scenarios.

- 2026-09-03 — **Analyzer Database Backpressure:** Reduced Activity analyzer defaults from 100 concurrent / 500-wallet batches to 20 concurrent / 100-wallet batches. Full snapshots can contain hundreds of thousands of events; the old fan-out left PostgreSQL processes blocked on `ClientWrite` and tuple locks. The analyzer remains parallel but now drains without overwhelming the database.

- 2026-09-03 — **Lineage Gate Paused:** Paused the historical per-wallet Alchemy transfer scan and lineage-ledger materializer. A full ERC-1155 wallet-history crawl is too expensive and incomplete under provider limits, while Polymarket `/activity` has no P2P-transfer event type. Worker 4 now analyzes every complete staged Activity snapshot without waiting for `lineage_baseline_complete`; existing transfer evidence is retained as optional provenance and no positions or metrics are rewritten.

- 2026-09-02 — **Activity/Lineage Worker Split:** Split the former combined Worker 3 into Activity snapshot backfill (3A) and per-wallet CTF lineage transfer backfill (3B). The analyzer remains gated on both completion flags; the orchestrator now supervises the split workers independently, allowing Activity API and Alchemy throughput to be tuned separately.

- 2026-09-02 — **Worker 3 Backlog Drain:** Activity backfill now uses a short configurable active-cycle delay (10 seconds by default) while candidates remain, retaining the 15-minute idle delay only when the queue is empty. Per-wallet lineage HTTP concurrency was raised conservatively from 20 to 30 and remains independently configurable; wallet task concurrency stays at 100 to avoid database connection exhaustion.

- 2026-09-02 — **Operations Runbook:** Added `docs/rule.md`, the canonical guide for worker ordering, standalone/orchestrator naming, PowerShell start commands, freshness and retention rules, Activity/lineage analysis gates, capacity limits, troubleshooting, and safe single-wallet recovery. It explicitly separates evidence classification from canonical metric eligibility and prohibits force-matching divergent data.

- 2026-09-02 — **Dormancy Last-Activity Recovery:** `last_trade_sweeper.py` now scans hibernated as well as active wallets; excluding hibernated rows created a stale-timestamp feedback loop that could never wake them. It prioritizes never-swept wallets and uses configurable 120-wallet / 30-request batches, staying beneath the shared Activity API budget alongside Worker 3.

- 2026-09-02 — **Activity Queue Cadence:** Reduced Worker 3's default idle interval from six hours to 15 minutes. Its bounded 150-wallet batch now progresses the divergence queue continuously while preserving the 100-way wallet concurrency and 50-request Activity HTTP guardrail.

- 2026-09-02 — **48-Hour All-Wallet Position Refresh:** Open and closed position backfillers now include hibernated/dormant wallets and select any wallet whose respective source has not completed in 48 hours (or never has). Defaults are configurable 100-way wallet concurrency (double the prior setting) with 1,000-wallet batches and 140+ connection pools, leaving capacity for the Activity workers; the shared PostgreSQL endpoint limiter remains the hard Data API request ceiling.

- 2026-09-02 — **Lineage Ledger Backfill:** Added supervised Worker 17 (`lineage_ledger_backfill_worker.py`), which uses resumable source-ID batches to materialize both incoming and outgoing records from canonical `wallet_position_transfers_v2` into uncapped `wallet_lineage_trades_v2`. The real-time position/funding tracker now writes both ledger records for every newly observed transfer. This repairs the pre-existing state where transfer evidence existed but the unified lineage trade table was empty; it does not alter positions or metrics.

- 2026-09-02 — **Lineage Transfer Evidence in Activity Reconciliation:** Added migrations `t3u4v5w6x7` and `u4v5w6x7y8` plus `lineage_transfer_backfill.py`. Worker 3 now completes both the immutable Activity snapshot and paginated incoming/outgoing CTF ERC-1155 transfer baseline before Worker 4 may analyze it. Worker 4 matches transfer evidence only by the exact stored `source_asset` token ID, records incoming transfer shares/count/timestamp, and marks the acquisition `verified_transfer_in` without inventing cost basis or changing PnL/win-rate eligibility. Transfer identity includes on-chain `log_index`, event type, and asset, preventing distinct same-transaction outcome-token logs from collapsing.

- 2026-09-02 — **Root Artifact Organization:** Moved stale root runtime logs to `logs/archive-root/`, one-off diagnostic and backtest Python files to `scratch/root-diagnostics/` and `scratch/root-backtests/`, exports to `scratch/root-exports/`, generated reports to `scratch/root-reports/`, and working wallet evidence to `scratch/evidence/`. These local archival folders are ignored by Git. The repository root now contains only project configuration and entry-point files; `README.md` defines the placement rule for future artifacts.

- 2026-09-02 — **Activity Split-Worker Evidence Preservation Repair:** `backfill_activity()` now stages the complete fetched snapshot until `analyze_activity()` has reconciled that exact snapshot; only the analyzer applies the 500-distinct-event hot-cache limit. High-divergence archives are written before pruning, scan state records `pending_snapshot_id`, DB rows are restored to the Activity API field shape before classification, and the backfiller now uses the approved $10k / $1k+10% divergence queue instead of all active wallets. Command-line batches are bounded to 150 backfills / 500 analyses, with Activity HTTP fan-out capped at 50 concurrent page requests to prevent connection-timeout failures; known pre-fix states are repaired before never-scanned wallets, and the orchestrator supervises the backfiller and analyzer separately as Workers 15 and 16. Added migration `s2t3u4v5w6`; verified `0xbc2635...` preserved and analyzed all 344/344 distinct source events with 54 matching BUY-leg rows. Requeued 65 pre-fix states without deleting their historical audits.

- 2026-09-02 — **Repository Cleanup:** Replaced the generated, over-broad `.gitignore` with focused rules for runtime state, local archives, editor caches, and exports, so source scripts and migrations are no longer silently hidden. Removed unused combined Activity launcher/worker (`launch_parallel.py`, `activity_reconciliation_worker.py`) now that Workers 15–16 are split, plus unreferenced legacy metric/reconciliation wrappers (`win_rate_compute.py`, `reconcile_diverging_wallets.py`). Canonical `positions_metrics_compute.py` and its modular core/category/window workers remain the only metric writer.

- 2026-09-02 — **Generated Artifact and Legacy-Test Cleanup:** Ignored both `backtest_cache/` and `backtest/_cache/`, logs, runtime cursors, exports, duplicate root reports, the top-level operator scratchpad, and ad-hoc source diagnostics; maintained commands live under `src/scripts/`. Removed obsolete schema-v1/import-time/manual API probes and the superseded worker-architecture test, then configured `pytest.ini` to collect only maintained `tests/`, excluding ignored scratch and script probes. Active worker logs remain in `logs/` locally and are never committed.

- 2026-09-02 — **Incremental Activity Evidence and Thresholded Reconciliation:** Added migration `r1s2t3u4v5` with lifecycle aggregates, scan watermarks, deduplicated exception events, Activity-only state, and Parquet archive manifests. `audit_position_activity_coverage.py` now records BUY/SELL/REDEEM/SPLIT/MERGE/CONVERSION/reward/rebate/yield facts, keeps only the newest 500 raw hot events per wallet, retains contradiction evidence separately, and resumes from a timestamp/hash watermark with overlap. Added `activity_reconciliation_worker.py` using `>= $10k` or `>= $1k and >=10%` divergence thresholds. Added `/api/v2/wallets/{address}/reconciliation` and a wallet Data Integrity tab. Canonical PnL eligibility remains unchanged.

- 2026-09-01 — **Bounded Normal-Trade Retention Set to 600:** `polymarket_trade_backfiller.py` now fetches and retains the newest 600 CLOB trades for normal wallets, removing the separate 15-day deletion rule. A new normal-wallet trade evicts only the oldest trade beyond the 600-row cap. Lineage trade, transfer, and funding evidence remains uncapped.

- 2026-09-01 — **Lineage Trade Backfill Schema Repair:** `polymarket_trade_backfiller.py` no longer inserts normal-trade columns into the incompatible lineage table. Migration `q0r1s2t3u4v5` adds source trade fields and a `(wallet_address, tx_hash, log_index)` idempotency index to `wallet_lineage_trades_v2`; the lineage path writes CLOB rows as `event_type='TRADE'` while preserving transfer/funding semantics. Historical trade tables were explicitly cleared before this repair, so no legacy-row conversion was attempted.

- 2026-09-01 — **Retained Activity Evidence and Four-Way Position Reconciliation:** `audit_position_activity_coverage.py --persist` now retains every fetched `/activity` event in `wallet_activity_events_v2`, writes per-position DB-versus-Activity shares, average-price, cost-basis, and PnL comparison facts to `wallet_position_activity_reconciliations_v2`, and records Activity-only market IDs in `wallet_activity_only_markets_v2`, including whether the ID is represented by an open position. The four classifications are `direct_activity_buy`, `activity_same_leg_nontrade`, `activity_outcome_differs`, and `activity_absent`; `comparison_quality` separately identifies exact BUY matches from value differences. All are audit marks only and do not alter eligibility or metrics. BreakTheBank has 38 IDs absent from closed history, 19 represented by open positions, and 19 absent from both tables.

- 2026-09-01 — **Explicit Deep Closed-History Operator:** Added `src/scripts/backfill_deep_closed_history.py`. It is fetch-only by default; `--apply` refuses incomplete Activity discovery or per-market closed-position retrieval, then upserts (never deletes) and recomputes only the selected wallet.

- 2026-09-01 — **Integrity Roster Completeness:** `run_wallet_integrity_scan.py` now retains wallets with zero eligible closed rows in its report instead of dropping them through a ledger-driven join. Cap state is explicitly `null` when the legacy schema has no source-completeness evidence; it is never inferred as uncapped. Activity escalation includes both metric-vs-ledger and root-category-vs-ledger inconsistencies.

- 2026-09-01 — **Bounded Fleet Integrity Scan:** Added `run_wallet_integrity_scan.py --all`, which composes address-keyset batches into one read-only global roster rather than issuing an oversized database-wide aggregation. No eligibility, metrics, or ledger rows are changed by this command.

- 2026-09-01 — **Source Worker Concurrency:** Raised `positions_open_backfill.py`, `positions_closed_backfill.py`, and legacy hibernated `positions_winrate_backfill.py` to 50 concurrent wallet tasks with 80-connection pools. The spare pool capacity prevents limiter-connection starvation; the shared PostgreSQL buckets still enforce the real Data API request budget. Updated `problem2.md` to distinguish code-complete safeguards from pending evidence-led historical rollout.

- 2026-09-01 — **Observable Fleet Scan:** The all-wallet integrity scan now writes a cumulative partial roster and `.status.json` after every keyset batch, including exact scanned/flagged counts and cursor. Interrupted read-only scans are observable without mistaking a partial roster for final.

- 2026-09-01 — **Active-Wallet Integrity Queue:** `run_wallet_integrity_scan.py` now excludes `wallets_v2.is_dormant=TRUE` by default. Dormant wallets remain stored and are available through `--include-dormant`, but no longer flood the active historical-recovery queue with intentionally stale source snapshots.

- 2026-09-01 — **Metrics Worker Active Boundary:** `positions_metrics_compute.py` now joins `wallets_v2` and excludes dormant wallets, matching the source-recovery roster. Its 150-way concurrency is retained for database-only aggregation; it does not validate source completeness.

- 2026-09-01 — **Provenance Self-Comparison Fix:** `wallet_provenance_audit.classify_row` now skips the position under audit when evaluating complementary siblings. A 50-cent row is mathematically its own complement but is not an opposite-leg signal. BreakTheBank’s earlier 16-row complementary classification must be rerun; no eligibility changes were made.

- 2026-09-01 — **Evidence Eligibility Foundation & Bounded Integrity Scan**: Added migration `m6n7o8p9q0r1` with default-true `wallet_closed_positions_v2.metrics_eligible`, row exclusion metadata, immutable source/evidence/audit tables, and a shared-rate-limit bucket. Core metrics, category metrics, historical windows, and normal closed-position API responses now honor the eligibility flag; no historical rows were excluded. The scheduled open/closed/redeemable backfillers acquire PostgreSQL token buckets before every `/positions`, `/closed-positions`, and combo `/activity` request. Added conservative outcome normalization/provenance helpers, `PostgresRateLimiter`, dry-run-first `apply_position_eligibility.py`, and `run_wallet_integrity_scan.py`, a resumable read-only aggregate scan bounded by wallet batch. `audit_position_activity_coverage.py --persist` stores immutable activity-snapshot hashes and conservative `eligible`/`review_required` decisions, never automatic exclusions. Deep closed-history recovery is available only as an explicit fetch option: it splits complete Activity history by timestamp, discovers condition IDs, then retrieves each condition through the authoritative closed-positions endpoint; standard workers remain conservative and mark an address-wide ceiling incomplete. Its first ten-wallet sample found eight stale metric-versus-ledger mismatches without writing any wallet data.

- 2026-09-01 — **Activity-to-Position Provenance Audit**: Added the read-only `src/scripts/audit_position_activity_coverage.py` diagnostic. It retrieves complete `/activity` history by recursively partitioning timestamp windows at the API offset ceiling, then reconciles stored closed-position rows at `(conditionId, outcome)` grain without changing any wallet data. Likebot (`0x03c3…3a1d`) audit found Activity coverage for every one of 40,492 stored position rows; 1,377 outcome-label mismatches require investigation rather than deletion. The audit exposed 1,382 mathematically complementary unproven rows left by the retired divergence reconciler (about -$137.6k) and separately proved that its displayed $4.646m `total_pnl` is stale versus the current row ledger's $2.478m. No metrics or ledger rows were rewritten.

- 2026-09-01 — **Complete Capped-Position Recovery & Additive Sports Breakdown**: Corrected `/positions` completeness handling at the documented offset-10,000 ceiling. Capped wallets now use the official accounting snapshot as the complete `(conditionId, asset)` inventory and refetch `/positions` in bounded `market` partitions to retain real cost-basis/PnL fields; snapshot/live drift is accepted only when `/closed-positions` proves the asset exited. Partial recovery remains `positions_complete=FALSE`, so stale-row pruning and sync advancement remain disabled. Added bounded shared request concurrency and focused completeness tests. Category computation now uses exact Data API event slugs for known sports families, corrects wrongly tagged non-sports markets, emits `Unclassified Sports` for the true residual, and stores event slugs during future closed/redeemable syncs. The wallet categories API now sums all league rows into each displayed subcategory instead of silently selecting only the highest-volume league. Recomputed only the two audited wallets; no fleet-wide metric/PnL rewrite was run.

- 2026-09-01 — **Backend Recovery & v2 Leaderboard Schema Compatibility**: Stopped stale pre-fix reconciliation/metric worker processes, restarted a wedged Docker Desktop/PostgreSQL runtime without deleting volumes, and restarted one clean API process. The API now passes `/health`, `/api/v2/leaderboard/global?limit=1`, and `/api/v2/leaderboard/wallets?limit=1`. Fixed `src/api/routers/leaderboard_v2.py` selecting and sorting by nonexistent `wallet_metrics_v2.avg_position_size`; `max_trade_size` now returns typed `NULL` because that metric is not stored, rather than fabricating a value or failing the leaderboard query. The first cold wallet-list query took about 21 seconds; subsequent cached access completed in 6.6 seconds.

- 2026-09-01 — **Row-Level Win Rates Restored & Guarded Global Rebuild**: Reversed condition-level win aggregation and removed all synthetic opposite-leg generation from `compute_core_metrics.py`, `compute_category_stats.py`, `compute_historical_windows.py`, `src/pnl/reconcile.py`, and `reconcile_diverging_wallets.py`. Every `(condition_id, outcome)` row is again an independent resolved position; overall, category, price-bucket, and parlay wins use the same `realized_pnl > 0` rule. Resolved-but-unclaimed rows (`is_redeemable=TRUE`) are included. Fixed partial-sell cost accounting in `positions_open_backfill.py` and legacy `positions_winrate_backfill.py`: `realizedPnl + currentValue - min(initialValue, totalBought × avgPrice)`. Actual `/closed-positions` settlement now flips `is_redeemable=FALSE`. Added a 249,545-record BreakTheBank `/activity` backtest: $1,720,229.20 recorded cash PnL + $907,920.86 open value = $2,628,150.06 versus $2,638,694.37 official PnL (0.40% residual), disproving `CONVERSION.size` as a cash debit. Added `backtest_global_position_fix.py`; seven wallet archetypes confirmed that independent position wins remain measurable while position PnL can legitimately diverge by tens of millions for minters. Official category leaderboard totals were exact for some wallets but stale/non-additive for others, so they are diagnostic only and never used to scale the ledger. Hardened `fetch_closed_positions` against failed page 0, partial concurrent batches, repeated/cached pages, duplicate rows, and the documented 100,000 offset ceiling. Reworked `repair_and_sync_wallet.py` to fetch/validate first, default to dry-run, reject incomplete snapshots, and replace closed/open rows plus metrics atomically only with `--apply`; it can process the divergence roster with `--divergence-file`. Converted `win_rate_compute.py` into a compatibility wrapper around the canonical modular pipeline, eliminating a competing win-rule writer. Added 42 passing focused tests. No fleet-wide recomputation or destructive cleanup was run.

- 2026-08-31 — **Decoupled Modular Metrics Compute Fleet & Global Progression Reconciliation — SUPERSEDED BY 2026-09-01 ROW-LEVEL FIXES**:
  * **Decoupled 3 Dedicated Standalone Workers:** Modularized metrics computation into `compute_core_metrics.py` (Worker A originally used paired win rates), `compute_category_stats.py`, and `compute_historical_windows.py`. Their metric grain and redeemable-row handling were corrected on 2026-09-01.
  * **Historical Progression Change (superseded):** This release excluded `is_redeemable=TRUE` rows and introduced condition-level pairing/scaling. The 2026-09-01 correction restores genuine resolved-but-unclaimed rows, independent position outcomes, and unscaled ledger values.
  * **Category Attribution Scaling:** Reconciled Root Category returns so $\sum \text{Root Categories} \equiv \text{Total PnL}$ (`Delta = $0.00`) across all wallets.
  * **Infrastructure Upgrade:** Increased PostgreSQL `max_connections` to 600 in `docker-compose.yml` to support 250+ concurrent background tasks with zero connection contention. Verified on `swisstony`, `ferrariChampions2026`, `BreakTheBank`, and `S-Works`.
  * **Eliminated $261.07 Million in Artificial Database Losses:** Executed `repair_phantom_losses.py` across `wallet_closed_positions_v2`. Repaired **81,546 zero-bought minted positions** (set realized PnL to `$0.00` with `data_quality_flag = 'minted_shares'`) and **4,461,099 mixed positions** (capped realized losses at actual CLOB cash spend with `data_quality_flag = 'mixed_minted_shares'`).
  * **Global Metrics Recomputation (Option B / Turbo Mode):** Executed `recompute_metrics.py` across **361,378 wallets** with 140 parallel async workers, re-aggregating clean `total_pnl = sum(realized_pnl)`, 10 rolling windows (`pnl_100`..`pnl_5000`), win rates, and category statistics into `wallet_metrics_v2` and `category_stats_v2`.
  * **Verified Whale Convergence (`BreakTheBank` `0xf031...` & `debased` `0x24c8...`):**
    - `BreakTheBank` (`0xf031...`): `total_pnl` aligned to **`+$3,092,881.90`** (matching Polymarket leaderboard #47 and on-chain cash ledger), recovering from stale `-$6.7M` DB loss, with an ascending positive progression curve.
    - `debased` (`0x24c8...`): `total_pnl` aligned to **`+$1,484,572.03`** (matching Polymarket leaderboard #115 on $516M volume), purging $50.8M in double-counted complete-set split losses.
  * **Worker & API Audit:** Audited all 14 orchestrated background workers, compute workers, and API endpoints (`wallets_v2.py`, `positions_metrics_compute.py`, `positions_open_backfill.py`, `positions_winrate_backfill.py`, `leaderboard_stats.py`) to confirm zero legacy `cashPnl` overwrite logic remains.

- 2026-08-28 — **Wallet Detail Page PnL Display Fix + Audit of `problem.md` Findings**:
  - **Frontend used broken `total_pnl` instead of `pm_pnl`:** `frontend/src/app/wallet/[address]/page.tsx` computed both `dbPnl` (`total_pnl`) and `pmPnl` (`pm_pnl` fallback to `total_pnl`) but the displayed `pnl` variable was hardcoded to `dbPnl`, so `pmPnl` was computed and unused. Changed `const pnl = dbPnl` → `const pnl = pmPnl ?? dbPnl`. Also fixed the "Last 5000+" PnL window fallback and the ROI% calculation to prefer `pm_pnl` over `total_pnl`. The `leaderboard_v2` API list endpoints already used `COALESCE(pm_pnl, total_pnl)` — only the single-wallet detail page was affected.
  - **Confirmed 3 wallets with severe `total_pnl` vs leaderboard mismatch:** `0x7c1ee865...` (DB positions sum diverges from leaderboard by millions), `0x84dbb710...`, `0xf0318c32...` (BreakTheBank, documented in `problem.md`, -$8.9M DB vs +$3.07M leaderboard/on-chain cash ledger).
  - **Root cause audit (see `problem.md` + `docs/learner.md` §13/§14):** `total_pnl` in `wallet_metrics_v2` is a straight sum of `wallet_closed_positions_v2.realized_pnl` (position-level accounting). Polymarket's real PnL is cash-flow based (`deposits/withdrawals/balance/open-value`). These are fundamentally different formulas and will never match exactly — `pm_pnl` (synced from the official leaderboard API) is the only trustworthy source for a wallet's true lifetime PnL. Confirmed still-live in code (not yet fixed): (1) `avg_buy_price` defaults to `0.50` when missing in `sync_redeemable_positions` (`positions_open_backfill.py:109-115`, `positions_winrate_backfill.py:117-124`) inflating synthesized `total_bought` and phantom losses on multi-outcome markets; (2) redeemable win/loss in `positions_metrics_compute.py:135-138` requires `avg_sell_price >= 0.95` for `is_redeemable=TRUE` rows, which is structurally impossible for unredeemed positions (`avg_sell_price` is always `0` since nothing was sold) — silently marking ALL redeemable/unclaimed wins as losses; (3) category & total volume fallback in `positions_metrics_compute.py:117,158,233` sums raw `total_bought` (share count) instead of `total_bought * avg_buy_price` (USD) when `pm_volume` is absent, inflating volume by the inverse of the average share price. None of these have been patched yet — tracked for the next backfill correction pass.
  - **Truncated backfill holes confirmed on all 3 audited wallets:** `wallet_closed_positions_v2` is missing thousands of positions per wallet due to an old silent-truncation bug (fixed 2026-08-26, see §11.5) that ran BEFORE the fix landed; the incomplete rows were already marked `closed_synced_at`, so incremental syncs early-stop on known keys and never backfill the holes. Requires a forced full re-fetch (reset `closed_synced_at=NULL` + delete existing rows) per affected wallet, not just a resync.

## [2026-08-28] - PnL Data Integrity & Backfill Engine Repair (v3)

### Fixed
- **Redeemable PnL Calculation (`positions_open_backfill.py`, `positions_winrate_backfill.py`)**:
  - Replaced naive `cashPnl = -(size * avgPrice)` with actual CLOB cash spend `-(totalBought * avgPrice)`.
  - Capped unredeemed losses at actual orderbook dollars spent, eliminating **$12.93M** in phantom losses from complete-set minted tokens (`size >> totalBought`) and transferred positions.
  - Added support for `data_quality_flag` (`minted_shares`, `mixed_minted_shares`, `missing_avg_price`, `missing_total_bought`).
- **Unified Win Detection (`positions_metrics_compute.py`, `win_rate_compute.py`)**:
  - Added `LEFT JOIN markets_v2 m ON c.condition_id = m.condition_id` to query canonical `winning_outcome`.
  - Replaced flawed `is_win = bool(is_redeemable)` row-source flag bug and `sell_p >= 0.95` fallback with `outcome == winning_outcome`.
- **Volume Calculation (`positions_metrics_compute.py`)**:
  - Fixed volume aggregation to sum USD dollars (`total_bought * avg_buy_price`) rather than raw token contract counts.
- **Repair Scripts (`repair_position_fields.py`)**:
  - Removed hardcoded `1.0` defaults and synthetic multipliers in accordance with the "Never Assume" principle.
- **Target Wallet Re-sync (`repair_and_sync_wallet.py`)**:
  - Repaired and re-synced `0x7c1ee8...` (recovered +$8.98M to +$2.26M, 5,258 bets), `0x84dbb7...` (recovered to +$1.50M, 52.64% win rate, 2,576 bets), and `0xf0318c...` (recovered +$11.24M to +$2.34M DB PnL / +$3.09M cash PnL).

- 2026-08-27 — **High-Performance Backfill & Rolling Window PnL Fixes**:
  - **Worker 3 Hidden Network Removals:** Removed `fetch_all_trades` and `fetch_website_pnl` HTTP calls from offline metrics computer (`positions_metrics_compute.py`). Total volume/PnL now derives directly from local database records (`wallet_metrics_v2` / `wallet_closed_positions_v2`).
  - **Worker 3 Concurrency & In-Loop Imports:** Moved title classification imports to top-level module scope, switched category statistics upserts to single-round-trip `executemany` statements, raised concurrency to 80 workers with a dedicated 110-connection asyncpg pool.
  - **Polymarket `/value` Fast-Path (Worker 4 & Stats Refresher):** Upgraded `fetch_balance` in `stats_refresher.py` and `pnl_balance_refetch.py` to check `/value` directly without falling back to serialized Alchemy RPC on $0 balance wallets, raising throughput from 4.5 w/s to 63+ w/s.
  - **Rolling Window PnL Recency Fix (`pnl_100` ... `pnl_3500`):** Excluded redeemable positions (`is_redeemable = TRUE`) from rolling window slices because their `closed_at` is set to market resolution `endDate` rather than trade execution date. Prevented historical resolution losses from clustering at the top of recent trading windows while preserving them in all-time win rates and `pnl_5000`.
  - **Closed Position Backfill Page 0 Fast-Path (Worker 2):** Integrated Page 0 fast-path in `fetch_closed_positions` (`wallet_trade_history.py`) to stop pagination at first encountered historical key, accelerating closed backfills to 35+ wallets/s.
  - **HyperSync 30-Day Active Traders Ingestion:** Reconciled 273,874 active 30-day Polygon traders against `wallets_v2`, flipped 16k dormant flags back to active (`is_dormant = FALSE`), and brought active database universe to 377,777 wallets.

- 2026-08-26 — **Comprehensive System Audit & Bug Fixes (Backend, API, DB, and Frontend)**:
  - **Orchestrator Self-Healing Watchdog:** Added per-worker heartbeat tickers and `touch_heartbeat(name)` so long-running continuous workers (`while True` loops) are not falsely killed and respawned mid-cycle every 6 minutes.
  - **Win-Rate Inflation & Redeemable Determination:** Dust-price positions (< $0.03) are strictly gated on `redeemable=True` for win consideration (otherwise classified as losses). Added `is_redeemable` selection and verification in standalone `positions_metrics_compute.py` worker.
  - **`is_redeemable` Clobbering & Dormancy Updates:** Excluded synthetic redeemable rows from `upsert_closed_positions_v2` to prevent overwriting `is_redeemable=TRUE` with `FALSE`. Updated `wallets_v2.last_trade_at` to use `GREATEST(COALESCE(last_trade_at, $2), $2)` so wallets with trades older than the 15-day retention cap do not lose their last active dates.
  - **Stale Position Prune Guard:** `fetch_positions` now tracks pagination completeness `(positions, is_complete)` and prevents `prune_stale_open_positions_v2` from deleting valid open positions on transient network/API errors. Fixed `(condition_id, asset)` wrap detection and combo BUY activity aggregation.
  - **WebSocket Datetime Serialization:** Serialized `wallet_activity_v2` timestamps and decimals prior to WebSocket broadcast to eliminate Starlette `TypeError: Object of type datetime is not JSON serializable` crashes.
  - **Database Connection & Query Optimization:** Safely normalized hostnames with `urllib.parse.urlsplit` (avoiding blind replace in passwords) and enabled dynamic SSL configuration (`DB_SSL` / `sslmode`). Refactored leaderboard transfer subqueries into `LEFT JOIN LATERAL` queries.
  - **Frontend Pagination & Seed Data:** Added `resetAllPages()` across all filters and search in `frontend/src/app/wallets/page.tsx`. Replaced mock seed data constants in price buckets (`?? 31, ?? 16...`), removed micro-USDC `>10000` division hack, fixed fallback tier badge, and added active address request guards.
  - **10-Window PnL Progression Mapping:** Fixed `HistoricalProgressionChart.tsx` and `/wallet/[address]/page.tsx` mapping `pnlWindows` to actual database columns (`pnl_100` .. `pnl_5000` instead of `pnl_window_10` .. `pnl_window_5000`), fixed standardLabels typo, removed `$274.6k` hardcoded fallback multiplier, and ensured numbers and deltas format properly on the chart.
  - **Sports Team, Prop & League Classification:** Upgraded `category_classifier.py` and `gamma_market_cache.py` with comprehensive team-name and prop recognition (MLB, NFL, NBA, WNBA, NHL, MMA/UFC, Soccer, Esports). Automatically classifies markets with full `(Category, Subcategory, League)` metadata, wipes stale `category_stats_v2` before upsert, and eliminates empty `—` subcategories for all major sports leagues.
  - **Category Win Rates Scope Selector (All / Positions / Parlays):** Added segmented scope sub-filters (`All`, `Positions`, `Parlays`) to the Category Win Rates tab on `/wallet/[address]`. Enables traders to inspect category win rates across regular single positions, multi-leg parlays, or total combined activity.
  - **Closed Parlay Entry Cost & Closing Date:** Inferred missing `entryCost` and `avgPrice` for redeemed multi-leg winning parlays when omitted by Polymarket's payout endpoint. Added the **Date** (closing/settlement date) column with interactive sorting to the Closed Parlays table.
  - **Worker 2 Retired:** Decommissioned `capital_metrics_backfill` (Worker 2) from `orchestrator.py` (down to 12 active workers) because the leaderboard-derived ROI formula (`pnl / volume * 100`) operates autonomously with 100% accuracy in Worker 1 (`positions_winrate_backfill.py`) and Worker 4 (`stats_refresher.py`).
  - **Interactive Lineage & Capital Flow Graph:** Replaced static transaction tables on `/wallet/[address]` with `LineageFlowGraph.tsx`, featuring a responsive visual node-and-edge flow diagram showing parent funders, incoming/outgoing P2P outcome tokens, internal USD wallet-to-wallet funding, and direct on-chain deposits, with an interactive transaction ledger toggle.
  - **Custom Lineage & Transfers Table Columns:** Upgraded the dedicated Lineage view tab on `/wallets` with specialized columns: `WALLET`, `FUNDING SOURCE & FUNDER`, `P2P TRANSFER VALUE`, `FUND TRANSFER VALUE`, `WIN %`, `PNL`, `BALANCE`, `OPEN POSITION`, and `LAST ACTIVE`, with server-side sorting on all transfer metrics.

- 2026-08-22 — **Position History Beyond-5k Optimization & Column Rename**: Applied Alembic migrations `k4l5m6n7o8p9` and `l5m6n7o8p9q0` adding 35 pre-computed cumulative columns to `wallet_metrics_v2` (`resolved_count_beyond_5k`, `winning_count_beyond_5k`, `losing_count_beyond_5k`, `win_rate_beyond_5k`, `total_volume_beyond_5k`, `total_pnl_beyond_5k`, `avg_buy_price_beyond_5k`, `closed_cost_total`, `category_stats_beyond_5k`, 18 price bucket `*_beyond_5k` columns, `pruned_min_closed_at`, `pruned_at`). Enforces canonical **Market Concluded Date (`endDate`)** sorting to prevent late payout redemptions from jumping to the top of recent trades. Worker taxonomy clarified across all docs (13 supervised workers in `orchestrator.py` + 3 standalone pipeline workers).

- 2026-08-20 — ROI formula simplified to `pnl / volume * 100`: removed all deposit/withdrawal/inherited capital complexity from ROI calculation. Previous approach used Alchemy on-chain tracking (~30% accurate) and Polymarket activity API (500-record limit) for deposits, both unreliable. New formula uses only `pm_pnl` (leaderboard API) and `total_volume` (leaderboard API) — both accurate. `total_pnl` overwrite bug fixed: redeemable-position loop was reusing `total_pnl` variable, overwriting wallet-level PnL with individual position PnL (ferrariChampions: $2.77M → -$486, jtwyslljy: $2.36M → $5K). Changed to `redeemable_pnl`. `capital_metrics_backfill.py` simplified — still updates deposits/withdrawals from activity API for informational purposes but ROI no longer depends on them. `leaderboard_stats.py` updated to same formula. Learner.md Section 9 added, Section 7 marked superseded. Trade retention & lineage separation: `wallet_trades_v2` capped at **500 trades per wallet** and **30-day max retention** (later tightened to **15 days**, see below). Lineage wallets route to `wallet_lineage_trades_v2` — no cap. `wallet_activity_v2` pruned to **24 hours**. VHDX compacted 452→242 GB. One-time cleanup: 72.9M excess trades + 31.9M old trades + 288k feed records deleted. `docker-compose.yml`: `shm_size: 256mb`, `shared_buffers=256MB`. Post-session tuning: trade retention tightened 30d→**15d**, backfill position caps raised 75k→**200k** (`MAX_POSITIONS`/`MAX_CLOSED` in `wallet_trade_history.py` + `leaderboard_stats.py`), closed-position pruning removed from worker, LOW_BALANCE tier filter removed from backfill query, staleness window 12h→60h→**84h**, DB indexes `idx_wmv2_address` + `idx_wt2_wallet_address` created.
- 2026-08-19 — Redeable separation: Added `is_redeemable BOOLEAN DEFAULT FALSE` and `resolved_at TIMESTAMPTZ` to `wallet_closed_positions_v2`; `is_resolved BOOLEAN DEFAULT FALSE` to `wallet_positions_v2`; `redeemable_count INT DEFAULT 0` and `redeemable_winning_count INT DEFAULT 0` to `wallet_metrics_v2`. Alembic migration `h1i2j3k4l5m6`. `sync_redeemable_positions` now sets `is_redeemable=TRUE` and `resolved_at=endDate` when writing resolved-but-unclaimed positions; `upsert_closed_positions_v2` sets `is_redeemable=FALSE` (real settlements flip the flag). Metrics computation now tracks `redeemable_count`/`redeemable_winning_count` separately from `resolved_count`/`winning_count`. `wallet_positions_v2` now stores `is_resolved` for open positions with `redeemable=True`. When a redeemable position gets claimed, the closed-positions API upsert overwrites with `is_redeemable=FALSE`.
- 2026-08-18 — Position backfill caps raised 30,000→75,000 and force re-backfill: `wallet_trade_history.py` `MAX_POSITIONS`/`MAX_CLOSED` 30k→75k; `leaderboard_stats.py` `MAX_POSITIONS`/`max_closed` 30k→75k; `wallets_v2.py` `/resolved-positions` 5k→75k cap; `backfill_missing_wins.py` offset cap 5k→75k; `positions_winrate_backfill.py` `WALLET_TIMEOUT` 30→300s and prune guard cap 30k→75k. `fetch_closed_positions` rewritten for **concurrent pagination** (5 pages/batch via `asyncio.gather`, `sortBy=TIMESTAMP&sortDirection=DESC` for stable ordering, 3-attempt per-page retry, 0.05s inter-batch delay) — previously unstable pagination produced truncated/incorrect counts (likebot 28,616 → 13,850 → 150). Verified the API hard-caps closed positions at 30,000 total (offset 30k → 0 rows) but open positions exceed 30k, so the 75k cap matters for `fetch_positions`/redeemable sync. Added `force_backfill_over5k.py`: directly calls `process_wallet_backfill` on the 1,003 active wallets with ≥5,000 stored closed positions, bypassing the `computed_at` staleness gate (their metrics were truncated by the old caps). Fixed a latent bug in `prune_stale_open_positions_v2` (`conn.execute()` returns a str tag, not a cursor — `cur.rowcount` raised AttributeError). Docs updated (CORE_LOGIC.md, learner.md §7)
- 2026-08-18 — Stale-position pruning: `positions_winrate_backfill.py` now deletes `wallet_positions_v2` rows the Polymarket API no longer returns (transferred-out positions), so a sender that re-transfers inherited positions no longer double-counts its `SUM(avg_price*size)` inherited capital against the recipient's. Guarded to only run on complete fetches (non-empty, < 30,000-position cap). Verified: `0xe1111800...` had 327 stale rows / $43,180 cost basis; 247 pruned on test. Docs updated (CORE_LOGIC.md, learner.md §7)
- 2026-08-18 — ROI for inherited (transferred-position) wallets: wallets funded via P2P position transfer (`funding_source='inherited_positions'`/`'internal_funded'`, `funded_by`, or `transferred_positions_count > 0`) now **add** the **cost basis of their received positions** to the ROI capital base (`SUM(avg_buy_price*total_bought)` closed + `SUM(avg_price*size)` open): `cap = max(peak_capital, deposits, inferred_cap) + inherited_cap`. Transferred shares are ERC-1155 tokens (never USDC) so they never appear in deposits/peak — the inherited portion is genuinely additive, including for **mixed wallets** (deposits AND transfers), replacing the previous pnl/volume profit-margin fallback and the old `organic capital < $10` gate. Internal wallet-to-wallet USDC funding is NOT added (Alchemy deposits already count all non-exchange USDC inflows). Verified: pure-inherited bwgringo 6.33%→18.1%, Gustav4 0.46%→6.1%, Kainvest 3.83%→12.1%; mixed wr0ngw4yb3tt0r −1.82%→−2.60%, Djdjdjekekek −1.11%→−0.88%. Applied in `positions_winrate_backfill.py` + `audit_and_recalc_metrics.py`; docs updated (CORE_LOGIC.md, learner.md §7)
- 2026-08-18 — Win-rate & backfill fixes: `/positions` API wrap-around detection (10,500 real positions for likebot, not 60k); win detection for redeemable positions now uses `currentValue > 0` and synthetic entries carry an `is_redeemable` flag to stop partial-sell profit re-classifying concluded losses as wins (coinman2: -78 false wins); robust `_parse_end` in `sync_redeemable_positions` (fixes 1970-01-01 endDate crash); verified 3 wallets (WagerWizard 70.4%, beachboy4 53.5%, coinman2 65.2%); confirmed leaderboard API (`?user=` form) is the official PnL source (likebot +$107.5k, rank 1838); docs: updated CORE_LOGIC.md caps, added learner.md
- 2026-07-16 — Tabbed `/wallets` page shipped (plan Phases 3–5): new `GET /api/v2/leaderboard/wallets` (+`/counts`) canonical tabbed endpoint; shared frontend primitives (`WalletTable`, `WalletCell`, `SourceBadges`+`MightCookBadge`, `Pagination`, `useWalletList`/`useCopyAddress`/`useWatchlistAdd`, canonical `formatAddress`/`timeAgo`); `/wallets` page with All/Standard/Low Balance/New/Hibernated tabs, per-tab counts, Active/Inactive strip, source pills, search; Might Cook page retired into a badge on the New tab; Hibernating page retired; `/wallets/global` → redirect; sidebar = Activity/Wallets/Curated/Custom Wallets/My Tracker; Curated page refit onto shared hooks; `/might-cook` + `/hibernating` API endpoints now delegate onto the canonical tab filters
- 2026-07-16 — v1→v2 wallet migration completed: all 6 workers write `wallets_v2`/`wallet_sources_v2`/`wallet_metrics_v2` only (`tracked_wallets`/`wallet_stats` now legacy-read-only); canonical tier state machine (DEAD/LOW_BALANCE/NEW/STANDARD/CURATED + `is_dormant`, MIGHT_COOK retired to badge) in `stats_refresher` + `wallet_trade_history` vetting gates; stats engine swapped to `leaderboard_stats_v2`; curated promotion via `tier='CURATED'` in `poly_leaderboard_sync`; API fixes: dedup of multi-source wallets in `/global`, `/global-wallets`, `/might-cook`, `/tracked` (scalar-subquery source + EXISTS filter), category-branch `active_days` column bug; docs refreshed
- 2026-07-16 — Custom Wallets UI & API, Database Query Optimizations (wallet_sources_v2 indexes), Trade Tracker Market Upserts, and Tiers validation
- 2026-07-12 — last_trade_at Corruption Fix + PnL/Tier Cleanup
- 2026-07-11 — Global Wallets Page: Missing Wallets & Zero Win Rates
- 2026-07-11 — Deep-Fetch Trade Shape Bugs (Missing `conditionId`, Zeroed Prices, Wrong Topic Operator)
- 2026-07-10 — Supabase Wallet Profile for Global Win Rate
- 2026-07-10 — Mega-Whale Deep Fetch & Proxy Wallet Discovery
- 2026-07-10 — Triangle Logic for Accurate Outcomes & Multi-Window Stats
- 2026-07-10 — Closed Positions Fetch Fix & Trade Metrics
- 2026-07-09 — Trade Tracker: Category/Subcategory Missing in Activity Feed
- 2026-07-09 — Deposit Tracker Deep Fix & System Audit
- 2026-07-08 — Leaderboard Redesign & Cleanup
- 2026-07-08 — Session 2: Global List, Curated, Might Cook, Tracker, Category Fix
- 2026-07-08 — Polymarket Leaderboard Sync & Curated Redesign
- 2026-07-11 — Hibernator & 7-Day Scheduled Queue Pipeline
- 2026-07-12 — Session: Might Cook vs Global List Threshold
